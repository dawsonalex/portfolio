# Site author / identity (drives the whoami header + status bar)
author = "Alex Dawson"
subtitle = "(M|Bre)aking Things"
#role = "Full Stack Developer"        # shown in the whoami header, before the location
location = "Nottinghamshire"
brand = "ad"                          # short initials in the tab-bar brand block
bio = "Software developer."

description = "A collection of posts and project pieces from software engineer Alex Dawson"

# Contact / social links. Rendered as bracketed [label] links in the header.
# `label` is the visible text; `link` the href; `handle` an optional title/tooltip.
contact_links = [
    {label = "github", link = "https://github.com/dawsonalex", handle = "dawsonalex"},
    {label = "rss", link = "/index.xml", handle = "feed"},
]

# URL to get project JSON on build
projectsURL = ""
